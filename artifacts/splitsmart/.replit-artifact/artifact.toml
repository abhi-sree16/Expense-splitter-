kind = "web"
previewPath = "/"
title = "SplitSmart"
version = "1.0.0"
id = "artifacts/splitsmart"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21716

[services.development]
run = "pnpm --filter @workspace/splitsmart run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/splitsmart", "run", "build" ]
publicDir = "artifacts/splitsmart/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21716"
BASE_PATH = "/"
